# Quality module

This module provides **data quality and drift checks** for feature tables.

- Missingness report
- Numeric summary
- Drift score via PSI-like metric (reference vs recent)

Used by CLI: `mfs quality`.
